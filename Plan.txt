1.业务逻辑的抽取:
    1.命名风格：约定大于配置 handle(处理)
        getIndexHandler

2.MVC:就是分层开发  M model 数据模型层  V view 视图层  C controll 逻辑层 

3.高内聚 能在自己内部完成的功能一定不要干扰外部
低耦合 尽可能地降低模块之间的耦合度

4.记错本：sql很重要，愈发千万不能写错
        如果使用 127.0.0.1/login/:id     除了路由模块这样写，其他id都要带实际的参数
